export const metadata = {
  title: "Next dev ESLint reproduction",
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
