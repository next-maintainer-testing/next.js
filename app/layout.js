export const metadata = {
  title: 'Next.js hash-link keyboard focus reproduction',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
