import React from 'react'
import { RedirectAsyncButton } from './redirect-async-button'

export default function Page() {
  return React.createElement('main', null, React.createElement(RedirectAsyncButton))
}
