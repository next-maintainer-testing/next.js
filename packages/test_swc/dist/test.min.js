(function () {
  'use strict';
  var o = {
    ShapeTextEditor: {
      getInstance: function () {
        return {
          isOpened: false,
          initEditor: function (editor) {
            console.log('Editor initialized:', editor);
          }
        };
      }
    }
  };
  function t(model) {
    this.model = model;
    this.connectTarget = null;
  }
  t.prototype.veV = function (t, V) {
    var n, i;
    t.JVV && (n = Mt(V),
      i = o.ShapeTextEditor.getInstance(),
      n && 1 === n.length && !i.isOpened && 0 === n[0].ttV && i.initEditor(n[0].JVV));
  };
  if (typeof module !== 'undefined' && module.exports) module.exports = t;
  if (typeof window !== 'undefined') window.t = t;
  function Mt(t) {
    let e = [];
    t.shapes.all().forEach(function (b) {
      if (b.isSelected()) e.push(b.ctV);
    });
    return e;
  }
})();
