This standalone reproduction mirrors the reporter's locked `simple-package` Git artifact at commit `da5369056afd4049bc8fac12e3ee9022a088f2e8`. The vendored package tarball declares `dist/index.js` and `dist/index.d.ts`, but those ignored build outputs are absent and the package has no install lifecycle script. `node verify.mjs` runs `next build` and detects the reported `Can't resolve 'simple-package'` failure.
