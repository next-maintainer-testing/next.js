# Next.js issue 60081 reproduction

This minimal App Router project imports a client module that exposes a component through the `Header.Root` composition pattern. The verification requests the server-rendered page and distinguishes the reported React Client Manifest failure from successful rendering.
