# Next.js issue 69446 reproduction

This minimal App Router page preserves the reporter's three client Suspense boundaries and measures their browser hydration times. Run `npm install`, `npm run dev`, and open the browser console.
