{
  "name": "next-78137-reproduction",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "next build && cp -r public .next/standalone/ && mkdir -p .next/standalone/.next && cp -r .next/static .next/standalone/.next/"
  },
  "dependencies": {
    "next": "15.3.0",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  }
}
